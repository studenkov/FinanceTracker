# OAuth2 Client Dependencies (OIDC_STATEFUL)

## When to add

OIDC_STATEFUL authentication type.

## Dependencies

### Boot 3.x

```
groupId: org.springframework.boot
artifactId: spring-boot-starter-oauth2-client
```

### Boot 4.x

```
groupId: org.springframework.boot
artifactId: spring-boot-starter-security-oauth2-client
```
